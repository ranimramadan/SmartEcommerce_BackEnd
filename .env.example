APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=smartecommerce_backend
DB_USERNAME=root
DB_PASSWORD=


SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=localhost
# لسانكتوم: الدومينات الموثوقة (الواجهة)
SANCTUM_STATEFUL_DOMAINS=localhost:3000,127.0.0.1:3000

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

STRIPE_SECRET=
STRIPE_PUBLISHABLE=
STRIPE_WEBHOOK_SECRET=

DOCS_ENABLED=true
DOCS_DISK=public
DOCS_PAPER=a4

DOCS_INVOICES_ENABLED=true
INVOICES_DISK=public
INVOICES_PAPER=a4
INVOICES_PATH=invoices
# INVOICE_FILENAME= (اتركيها فاضية ليستخدم رقم الفاتورة)

# 3) متى “نبدّل” من .env إلى الداشبورد (PaymentProvider)؟

# اليوم: نشتغل بـ .env فقط. StripeGateway::boot() أصلاً يقرأ من مزوّد الجدول أولاً، ثم يسقط على config('services.stripe.secret') (يعني من .env). فكل شي شغّال الآن.

# بكرا (لما تبني شاشة إعدادات):

# أنشئي سطر بجدول payment_providers:

# \App\Models\PaymentProvider::updateOrCreate(
#   ['code' => 'stripe'],
#   ['name' => 'Stripe','is_active' => true,'config' => [
#       'secret_key'      => 'sk_live_***',
#       'publishable_key' => 'pk_live_***',
#       'webhook_secret'  => 'whsec_***',
#   ]]
# );


# لازم تحدّثي StripeGateway::handleWebhook() ليفضّل مفتاح الويبهوك من الجدول أيضاً (لو حبيتي):

# $provider = \App\Models\PaymentProvider::where('code','stripe')->where('is_active',true)->first();
# $endpointSecret = $provider?->getConfigValue('webhook_secret') ?: config('services.stripe.webhook_secret');


# اتركي مفاتيح .env كنسخة احتياطية (fallback) — ما بتكسر شيء.
